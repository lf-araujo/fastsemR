% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{fastsem_load}
\alias{fastsem_load}
\title{Load a locally built fastsem shared library.}
\usage{
fastsem_load(lib_path = NULL)
}
\arguments{
\item{lib_path}{Character.  Path to:
\itemize{
\item \code{libfastsem_r.so} on Linux,
\item \code{libfastsem_r.dylib} on macOS, or
\item \code{libfastsem_r.dll} on Windows.
}

When \code{NULL} (default) the function looks for any of those file names in
the current working directory.}
}
\value{
Invisibly \code{TRUE} on success.
}
\description{
Use this when you have compiled the fastsem library yourself (e.g. with
\verb{nimble buildRnim} in the fastsem source tree) and want to use that build
instead of the downloaded release binary.  Also useful for development and
testing without internet access.
}
\examples{
\dontrun{
# Point to a local build
fastsem_load("~/fastsem/libfastsem_r.so")

# Load from the current working directory (default search)
fastsem_load()
}
}
\seealso{
\code{\link[=fastsem_install]{fastsem_install()}} to download a pre-built release binary.
}
