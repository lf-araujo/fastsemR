% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastsem.R
\name{fastsem_fit}
\alias{fastsem_fit}
\title{Fit a structural equation model from lavaan syntax.}
\usage{
fastsem_fit(syntax, data, col_names = NULL, control = list())
}
\arguments{
\item{syntax}{Character string of lavaan model syntax.}

\item{data}{Data frame or numeric matrix.  Non-numeric columns are coerced
to \code{NA}.  Row order is preserved.}

\item{col_names}{Character vector of column names.  Defaults to
\code{colnames(data)}.  Must include every observed variable named in
\code{syntax}.}
}
\value{
A named list with the following elements:
\describe{
\item{\code{estimator}}{Character. \code{"ML"}, \code{"FIML"}, or \code{"DWLS"}.}
\item{\code{seType}}{Character. SE method, e.g. \code{"OIM"} or \code{"cluster-robust"}.}
\item{\code{nobs}}{Integer. Number of observations (or effective N for weighted
models).}
\item{\code{nFreeParams}}{Integer. Number of free parameters.}
\item{\code{df}}{Integer. Model degrees of freedom.}
\item{\code{chi2}}{Numeric. Chi-square test statistic.}
\item{\code{pvalue}}{Numeric. Chi-square p-value.}
\item{\code{neg2logL}}{Numeric. \eqn{-2 \ln L} value.}
\item{\code{aic}}{Numeric. Akaike information criterion.}
\item{\code{bic}}{Numeric. Bayesian information criterion.}
\item{\code{paramNames}}{Character vector. Structural names of all free
parameters (e.g. \code{"f->y1"}, \code{"Var(y1)"}, \code{"y1~1"}).}
\item{\code{estimates}}{Numeric vector. Parameter estimates in the same
order as \code{paramNames}.  Variance parameters are on the original
(not log) scale.}
\item{\code{se}}{Numeric vector. Standard errors.}
\item{\code{z}}{Numeric vector. z-statistics (\code{estimates / se}).}
\item{\code{stdEst}}{Numeric vector. StdAll standardised estimates.}
\item{\code{stdSE}}{Numeric vector. Delta-method SEs for \code{stdEst}.}
\item{\code{groupEstimates}}{List of numeric vectors (multi-group models).
\code{groupEstimates[[g]]} contains the per-group estimates for group
\code{g} in the shared parameter space.}
\item{\code{groupSEs}}{List of numeric vectors (multi-group models).}
}
}
\description{
The workhorse function of fastsemR.  Passes lavaan model syntax and a
data matrix to the fastsem compiled engine and returns a named list of fit
statistics and parameter estimates.
}
\section{Estimators}{

The estimator is chosen automatically based on the data and model:
\itemize{
\item \strong{ML} — complete data, no definition variables, no ordinal indicators.
\item \strong{FIML} — missing data (\code{NA}) present, or definition variables used.
\item \strong{DWLS / WLSMV} — ordinal indicators declared with \verb{ordered:} or
threshold syntax (\code{y | t1 + t2}).
}
}

\section{Extended lavaan syntax}{

Beyond standard lavaan, fastsem supports:
\itemize{
\item \strong{Parameter labels} — \code{b1*var}: assign the label \code{b1} to the path.
\item \strong{Equality constraints} — two params with the same label are equated,
or \code{b1 == b2} on a standalone line.
\item \strong{Bounds} — \code{b1 > 0.5} / \code{b1 < 2.0}.
\item \strong{Starting values} — \code{start(0.8)*var}.
\item \strong{Derived parameters} — \code{indirect := a * b} (delta-method SE).
\item \strong{Definition variables} — \code{data.colname * var} on the RHS of \verb{=~}.
\item \strong{Multi-group} — \code{group: colname} directive plus \code{c(v1, v2)*var}
per-group annotations.
\item \strong{Weighted observations} — \code{weight: colname}.
\item \strong{Cluster-robust SEs} — \code{cluster: colname}.
}
}

\examples{
\dontrun{
# Single-factor CFA on simulated data
set.seed(1)
n  <- 200
f  <- rnorm(n)
df <- data.frame(y1 = 0.8*f + rnorm(n, sd=0.6),
                 y2 = 0.9*f + rnorm(n, sd=0.5),
                 y3 = 0.7*f + rnorm(n, sd=0.7))

syntax <- "
  f =~ 1*y1 + y2 + y3
  f ~~ f
  y1 ~~ y1;  y2 ~~ y2;  y3 ~~ y3
"
res <- fastsem_fit(syntax, df)
print_fastsem(res)

# Mediation with derived indirect effect
df2 <- as.data.frame(scale(mtcars[, c("mpg","hp","wt")]))
syntax2 <- "
  wt  ~ a*hp
  mpg ~ b*wt + c_prime*hp
  hp ~~ hp;  wt ~~ wt;  mpg ~~ mpg
  hp ~ 1;  wt ~ 1;  mpg ~ 1
  ab := a * b
"
res2 <- fastsem_fit(syntax2, df2)
cat("Indirect hp->wt->mpg:", res2$estimates[res2$paramNames == "ab"],
    "+/-", res2$se[res2$paramNames == "ab"], "\n")
}
}
\seealso{
\code{\link[=print_fastsem]{print_fastsem()}} to display the result;
\code{\link[=run_fastsem]{run_fastsem()}} for the umx/OpenMx bridge.
}
