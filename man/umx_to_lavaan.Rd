% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastsem.R
\name{umx_to_lavaan}
\alias{umx_to_lavaan}
\title{Convert a umx / OpenMx RAM model to lavaan syntax.}
\usage{
umx_to_lavaan(model, include_map = FALSE)
}
\arguments{
\item{model}{An \code{MxModel} as returned by \code{umxRAM()} or \code{mxModel()}.}

\item{include_map}{Logical (default \code{FALSE}).  When \code{TRUE} returns a list
with two elements:
\describe{
\item{\code{syntax}}{The lavaan syntax string.}
\item{\code{param_map}}{Named character vector mapping fastsem structural
names (e.g. \code{"f->y1"}) to OpenMx free-parameter labels
(e.g. \code{"g_to_y1"}).  Used by \code{\link[=run_fastsem]{run_fastsem()}} for inject-back.}
}}
}
\value{
Character string (default) or named list when \code{include_map = TRUE}.
}
\description{
Inspects the RAM matrices (A, S, M) of an \code{MxModel} and generates the
extended lavaan syntax string that \code{\link[=fastsem_fit]{fastsem_fit()}} expects.  This is
called internally by \code{\link[=run_fastsem]{run_fastsem()}} but is also useful for inspection
or for further hand-editing before fitting.
}
\section{Translations performed}{

\describe{
\item{A matrix (directional)}{
Latent → manifest paths become \code{factor =~ indicator} lines.
Latent → latent become \code{~} regression lines.
Manifest → manifest become \code{~} regression lines.
A label starting with \code{"data."} is treated as a definition variable
and emitted as \code{data.colname * indicator}.
}
\item{S matrix (symmetric)}{
Free diagonal entries → \code{var ~~ var} (with \code{start()} or label if set).
Fixed non-zero diagonal entries → \code{var ~~ value*var}.
Free off-diagonal → \code{a ~~ b} covariance.
}
\item{M matrix (means)}{
Free entries → \code{var ~ 1} (with \code{start()} if the starting value is
non-zero).
Fixed non-zero entries → \code{var ~ value*1}.
}
\item{Bounds}{
\code{S@lbound} / \code{S@ubound} / \code{A@lbound} / \code{A@ubound} → \code{label > lo} /
\code{label < hi} lines.  Variance lower bounds of 0 are suppressed
because fastsem's log-variance parameterisation already prevents
negative variances.
}
\item{Equality constraints}{
Labels appearing on more than one free parameter are emitted as
\code{label*var}, which fastsem treats as an equality constraint.
}
}
}

\examples{
\dontrun{
library(umx)
df <- as.data.frame(scale(iris[, c(1, 3, 4)]))
names(df) <- c("sl", "pl", "pw")

m <- umxRAM("CFA",
  umxPath(from = "g", to = c("sl", "pl", "pw")),
  umxPath(v1m0 = "g"),
  umxPath(var  = c("sl", "pl", "pw")),
  data = df, autoRun = FALSE
)

# Inspect the generated syntax
cat(umx_to_lavaan(m))

# Include the parameter map for custom inject-back
out <- umx_to_lavaan(m, include_map = TRUE)
str(out$param_map)
}
}
\seealso{
\code{\link[=run_fastsem]{run_fastsem()}} to fit and inject back in one step.
}
