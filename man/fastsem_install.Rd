% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{fastsem_install}
\alias{fastsem_install}
\title{Install or re-install the fastsem native binary.}
\usage{
fastsem_install(force = FALSE)
}
\arguments{
\item{force}{Logical (default \code{FALSE}).  When \code{TRUE}, re-download even if a
cached binary already exists.  Use after a new fastsem release.}
}
\value{
Invisibly, the absolute path to the loaded binary.
}
\description{
Downloads the pre-built shared library for your platform from the fastsem
GitHub Releases page and loads it into the current R session.  The binary
is cached in \code{tools::R_user_dir("fastsemR", "cache")} and re-used on
subsequent calls (including package startup via \code{\link{.onLoad}}).
}
\details{
Platform detection is automatic:
\itemize{
\item Linux x86-64  → \code{libfastsem_r-linux-x86_64.so}
\item macOS x86-64  → \code{libfastsem_r-macos-x86_64.dylib}
\item macOS arm64   → \code{libfastsem_r-macos-arm64.dylib}
\item Windows x64   → \code{libfastsem_r-windows-x64.dll}
}
}
\examples{
\dontrun{
# First-time install (or after a fresh R install)
fastsem_install()

# Upgrade to the latest release
fastsem_install(force = TRUE)
}
}
\seealso{
\code{\link[=fastsem_update]{fastsem_update()}} for a one-liner force-download;
\code{\link[=fastsem_load]{fastsem_load()}} to use a locally compiled binary.
}
