% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastsem.R
\name{umx_to_fastsem}
\alias{umx_to_fastsem}
\title{Fit a umx model and return the raw fastsem result list.}
\usage{
umx_to_fastsem(model, data = NULL, ...)
}
\arguments{
\item{model}{An \code{MxModel} object (from \code{umxRAM()} etc.).}

\item{data}{Data frame of observed data.  Defaults to
\code{model@data@observed} when \code{NULL}.}

\item{...}{Reserved for future use.}
}
\value{
Named list as returned by \code{\link[=fastsem_fit]{fastsem_fit()}}, with an additional
\verb{$lavaan_syntax} element containing the translated lavaan syntax string
for inspection.
}
\description{
A lower-level alternative to \code{\link[=run_fastsem]{run_fastsem()}} that returns the raw fastsem
result list without injecting estimates back into the model object.  Useful
for inspecting fit statistics or parameter tables before committing to the
OpenMx model representation, or when you do not need the full inject-back
machinery.
}
\examples{
\dontrun{
library(umx)
df <- as.data.frame(scale(mtcars[, c("mpg","hp","wt")]))

m <- umxRAM("path",
  umxPath(from = c("hp","wt"), to = "mpg"),
  umxPath(cov   = c("hp","wt")),
  umxPath(var   = c("hp","wt","mpg")),
  umxPath(means = c("hp","wt","mpg")),
  data = df, autoRun = FALSE
)
res <- umx_to_fastsem(m)
cat("lavaan syntax used:\n", res$lavaan_syntax, "\n")
cat("chi2 =", res$chi2, "  df =", res$df, "\n")
}
}
\seealso{
\code{\link[=run_fastsem]{run_fastsem()}} for the full inject-back workflow;
\code{\link[=fastsem_fit]{fastsem_fit()}} for fitting from raw lavaan syntax.
}
