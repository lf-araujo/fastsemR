% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastsem.R
\name{run_fastsem}
\alias{run_fastsem}
\title{Fit a umx / OpenMx RAM model with fastsem and return the updated model.}
\usage{
run_fastsem(model, control = list(), engine = c("auto", "ram", "lavaan"))
}
\arguments{
\item{model}{An \code{MxModel} as returned by \code{umxRAM()} (or a bare \code{mxModel}
container holding submodels for multi-group use).  The model must have
raw (row-level) data in \code{model@data@observed}.}

\item{control}{Optional named list passed to the optimizer.  Recognized
fields: \code{maxIter} (int), \code{tol} (gradient-norm tolerance),
\code{ftol} (objective-change tolerance), \code{tryHard} (extra retry rounds on
non-convergence; each round multiplies \code{maxIter} by 3 and divides \code{tol}
by 10).  Any field absent or \code{0}/\code{0.0} uses the core default.}

\item{engine}{One of \code{"auto"}, \code{"ram"}, \code{"lavaan"}.  For multi-group
models, \code{"auto"} selects the RAM engine.  For single-group models, the
lavaan engine is always used (RAM single-group is not yet wired in the
R wrapper).}
}
\value{
The same \code{MxModel} object with:
\describe{
\item{Matrix values updated}{A, S, and M matrices hold the fastsem
point estimates.}
\item{\verb{@output} populated}{\verb{$estimate}, \verb{$standardErrors}, \verb{$fit}
(\verb{-2lnL}), \verb{$Chi}, \verb{$ChiDoF}, plus \verb{$.fastsem_*} slots for the
raw fastsem result list.}
\item{\verb{@.wasRun} set to \code{TRUE}}{Makes \code{summary()} and \code{umxCompare()}
work.}
}
}
\description{
The primary high-level function for users who work with \code{umxRAM()} models.
It:
\enumerate{
\item Translates the model to lavaan syntax via \code{\link[=umx_to_lavaan]{umx_to_lavaan()}}.
\item Extracts raw data from \code{model@data@observed}.
\item Fits the model with \code{\link[=fastsem_fit]{fastsem_fit()}}.
\item Writes estimates back into the model matrices and populates
\code{model@output} so that \code{summary()}, \code{omxGetParameters()}, and
\code{umxCompare()} work as if \code{mxRun()} had been called.
}
}
\section{Multi-group models}{

When \code{model@submodels} is non-empty, \code{\link[=run_fastsem]{run_fastsem()}} fits the joint
multi-group model and injects per-group estimates back into each submodel.
Two engines are available (selected via \code{engine}):
\itemize{
\item \strong{\code{"ram"}} (default for multi-group) sends the OpenMx RAM matrices
(A, S, optionally M) directly to fastsem's Nim core via
\code{fitSemFromMatricesMultigroupR}.  This bypasses the lavaan-syntax
round-trip and honors cross-group fixed covariances exactly (e.g.
cross-twin A=1.0 in MZ vs 0.5 in DZ).
\item \strong{\code{"lavaan"}} stacks data with a \code{fastsem_grp} indicator, builds a
combined lavaan syntax with \code{c()} per-group annotations, and fits via
\code{\link[=fastsem_fit]{fastsem_fit()}}.  Use this when the RAM engine errors on a model that
uses features it does not yet support (definition variables, ordinal
thresholds, configural cross-group different-label free parameters).
Parameters that carry the same OpenMx label in all submodels are equated;
others are group-specific.
}
}

\examples{
\dontrun{
library(umx)

# ── CFA ──────────────────────────────────────────────────────────────────
df <- as.data.frame(scale(iris[, c(1, 3, 4)]))
names(df) <- c("sl", "pl", "pw")

m <- umxRAM("CFA",
  umxPath(from = "g", to = c("sl", "pl", "pw")),
  umxPath(v1m0 = "g"),
  umxPath(var  = c("sl", "pl", "pw")),
  data = df, autoRun = FALSE
)
m_fit <- run_fastsem(m)
summary(m_fit)
omxGetParameters(m_fit)

# ── Multi-group CFA ───────────────────────────────────────────────────────
data(HolzingerSwineford1939, package = "lavaan")
hs <- HolzingerSwineford1939

mg_p <- umxRAM("mg_p",
  umxPath(from = "g", to = c("x1","x2","x3")),
  umxPath(v1m0 = "g"), umxPath(var = c("x1","x2","x3")),
  umxPath(means = c("x1","x2","x3")),
  data = hs[hs$school == "Pasteur", c("x1","x2","x3")]
)
mg_gw <- umxRAM("mg_gw",
  umxPath(from = "g", to = c("x1","x2","x3")),
  umxPath(v1m0 = "g"), umxPath(var = c("x1","x2","x3")),
  umxPath(means = c("x1","x2","x3")),
  data = hs[hs$school == "Grant-White", c("x1","x2","x3")]
)
r <- run_fastsem(mxModel("HS_mg", mg_p, mg_gw))
omxGetParameters(r@submodels$mg_p)
}
}
\seealso{
\code{\link[=umx_to_lavaan]{umx_to_lavaan()}} for syntax inspection;
\code{\link[=umx_to_fastsem]{umx_to_fastsem()}} for the raw result list without inject-back;
\code{\link[=fastsem_fit]{fastsem_fit()}} for fitting from raw lavaan syntax.
}
